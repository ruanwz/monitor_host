%h1 Home#index
- unless user_signed_in?
  %p= link_to 'Sign up as User', new_user_registration_path
  %p= link_to 'Sign in as User', new_user_session_path
- if user_signed_in?
  %p= link_to 'Host List', hosts_path

